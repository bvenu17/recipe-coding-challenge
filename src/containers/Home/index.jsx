import React, { Fragment } from 'react';

// child components
import Banner from './components/Banner';

const Home = () => {
  return (
    <Fragment>
      <Banner />
    </Fragment>
  );
};

export default Home;
